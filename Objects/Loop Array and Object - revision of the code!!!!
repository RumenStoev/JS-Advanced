function solve(arr) {



    for(let i = 0; i< arr.length; i++) {
        let allArr = arr[i].split("/");

        let currentHero = allArr[0];
        let currentHeroLevel = +allArr[1];
        let currentItems = allArr[2].split(", ");

         let currentHeroItems = [];
         if(allArr.length > 2) {
             currentItems = allArr[2].split(", ")
         }

            }

   let heroData = []
    herous.push(heroData)
    let herous = {
        name: currentHero,
        level:currentHeroLevel,
        items:currentItems

    };

    console.log(JSON.stringify(herous))
}



console.log(
    solve([
        'Isacc / 25 / Apple, GravityGun',
        'Derek / 12 / BarrelVest, DestructionSword',
        'Hes / 1 / Desolator, Sentinel, Antara'
    ])
)
