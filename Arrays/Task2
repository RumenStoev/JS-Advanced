function solve(nums) {
  let result = "";
   
    for(let i= 0; i<= nums.length; i++) {
      if(i % 2 === 0) {
      result+= nums[i] + " ";
    }
    }
  return result.trim()
}

console.log(
  solve(['20','30','40'])
)

->20,40
